<script lang="ts" setup></script>

<template>
    <div class="flex bg-[var(--color-element)] border border-[var(--color-border)] text-[var(--color-text)] rounded-xl cursor-pointer relative z-10">
        <button class="p-1 grow select-none">
            <span class="material-symbols-outlined text-[var(--color-text-alt)] block p-1.5 border border-[var(--color-border)] rounded-lg"> delete </span>
        </button>

        <button class="p-1 grow select-none">
            <span class="material-symbols-outlined text-[var(--color-text-alt)] block p-1.5 border border-[var(--color-border)] rounded-lg"> delete </span>
        </button>
    </div>
</template>

<style scoped></style>
